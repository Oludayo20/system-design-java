package com.systemdesign.ecommarketplace.orders.entity;

/** Mirrors the OrderStatus enum in src/modules/orders/entities/order.entity.ts. */
public enum OrderStatus {
  PENDING,
  SETTLED,
  CANCELLED
}
