<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>com.systemdesign</groupId>
  <artifactId>loyalty-consumer</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>loyalty-consumer</name>
  <description>
    FreshCart loyalty-consumer: added on day 2, after order-api, inventory-consumer,
    notification-consumer, and analytics-consumer already existed in production — binding a new
    queue to the exchange order-api had already been publishing to required zero changes
    anywhere else. Awards loyalty points idempotently, tracking processed eventIds so a
    redelivered duplicate never double-awards. See scripts/SimulateDuplicateDeliveryApp for the
    live proof.
  </description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-amqp</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.amqp</groupId>
      <artifactId>spring-rabbit-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>loyalty-consumer</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!--
        Lets scripts.SimulateDuplicateDeliveryApp run as a plain Java program (its own
        com.rabbitmq.client connection, no Spring context) — mirrors how
        03-async-queue-processing's Kafka scripts are run, and how the TypeScript sibling runs
        scripts/simulate-duplicate-delivery.ts via ts-node/npm script. Not bound to any build
        phase; invoked explicitly:
          mvn compile exec:java -Dexec.mainClass=com.systemdesign.freshcart.loyaltyconsumer.scripts.SimulateDuplicateDeliveryApp
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.4.1</version>
      </plugin>
    </plugins>
  </build>
</project>
