# --- App ---
PORT=3011

# --- PostgreSQL ---
# One instance, single schema (books, members, loans).
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=library
POSTGRES_PASSWORD=library_dev_password
POSTGRES_DB=library
